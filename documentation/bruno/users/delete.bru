meta {
  name: Delete User
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/users/:id
  body: none
  auth: bearer
}

params:path {
  id: paste-user-id-here
}

auth:bearer {
  token: {{accessToken}}
}
