meta {
  name: Update Team Category
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/team-categories/:id
  body: json
  auth: bearer
}

params:path {
  id:
}

body:json {
  {
    "label": "U16"
  }
}

auth:bearer {
  token: {{accessToken}}
}
