services:
  postgres:
    image: postgres:17
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: uclub
      POSTGRES_PASSWORD: uclub
      POSTGRES_DB: uclub
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
